package myPage;

public class MemberDeleteService {

	private MemberChangeDao memberChangeDao;
	
	/*
	 * @Transactional public void MemberDelete(String password) { Member member =
	 * memberChangeDao.selectByPassword(password);
	 * 
	 * if (member.getMem_password() != password) throw new
	 * WrongIdPasswordException();
	 * 
	 * member.selectByPassword(password);
	 * 
	 * memberChangeDao.Memberdelete(member); }
	 */
	
		
		
	}

